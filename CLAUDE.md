# brreg-mcp-server

See README.md for usage; tests via `npm test`, build via `npm run build` (dist is gitignored).

## Operating defaults

Frank's 10 Commandments (BrainOS `CLAUDE.md`) apply in every session — auto-loaded
globally via `~/.claude/CLAUDE.md` (which imports them). On conflict, the Commandments
win; lower law number first.
